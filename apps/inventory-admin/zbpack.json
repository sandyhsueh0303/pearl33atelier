{
  "build_command": "corepack enable && pnpm install --frozen-lockfile && pnpm build:inventory-admin",
  "start_command": "corepack enable && pnpm start:inventory-admin",
  "install_command": "corepack enable && pnpm install --frozen-lockfile"
}
