{
  "build_command": "corepack enable && pnpm install --frozen-lockfile && pnpm build:public-web",
  "start_command": "corepack enable && pnpm start:public-web",
  "install_command": "corepack enable && pnpm install --frozen-lockfile"
}
